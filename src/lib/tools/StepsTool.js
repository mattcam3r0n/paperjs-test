import Tool from "./Tool";
import ToolNames from './ToolNames';

export default class StepsTool extends Tool {
    constructor() {
        super(ToolNames.STEPS);
    }

    activate() {
        super.activate();
    }

    dispose() {
        super.dispose();
    }
}